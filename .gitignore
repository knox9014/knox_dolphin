# dependencies
/node_modules

# next.js
/.next
/out

# local memory database (never commit)
*.db
*.db-journal
*.db-wal
*.db-shm
/data

# secrets — keys live ONLY in .env, never in git
.env
.env.local

# misc
*.tsbuildinfo
next-env.d.ts
