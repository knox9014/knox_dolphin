# Copy this file to .env and fill in your own key.
# .env is git-ignored. Never commit real keys.

# Needed only from the extraction step (build order #4) onward — NOT for scaffolding.
ANTHROPIC_API_KEY=

# Local SQLite database file path (optional; defaults to ./data/knox.db)
KNOX_DB_PATH=./data/knox.db
